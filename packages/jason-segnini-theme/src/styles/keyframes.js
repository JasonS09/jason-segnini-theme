import {keyframes} from "frontity"

export const expandWidth = keyframes`
    from {width: 0;}
    to {width: 100%;}
`

export const expandHeight = keyframes`
    from {height: 0;}
    to {height: 100%;}
`